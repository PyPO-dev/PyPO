import numpy as np

##
# @file
# Set global world parameters for PyPO.

ORIGIN      = np.zeros(3)
XAXIS       = np.array([1, 0, 0])
YAXIS       = np.array([0, 1, 0])
ZAXIS       = np.array([0, 0, 1])
INITM       = np.eye(4)
